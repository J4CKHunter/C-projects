#include <stdio.h>
#include <stdlib.h>

int main()
{
   int dizi[3]={1,2};

   printf("%d",dizi[0]);
   printf("\n%d",dizi[1]);
   printf("\n%d",dizi[2]);

   /* dizilerde dizinin boyutunu belirleriz.mesela 10.bellekte bize bir degerden baslayıp (int dedigimiz için 4)4bitlik artıslarla
   devam eden sekilde 10 adet yer ayrılır.biz icine bisey atsak da atmasak da bu adresler dizi icin ayrılır.
   bu nedenle diziler bir pointerdır.pointerlar da dizidir. */

}
